# Copy to .env for local development
# API keys are NOT stored here in production — they go in the app settings (encrypted)
# This is only for dev overrides if needed
ELECTRON_RENDERER_URL=http://localhost:5173
